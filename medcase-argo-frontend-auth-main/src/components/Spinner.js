import { Fragment } from "react"
import spinner from "./M_Logo_Alpha_Favicon.gif"

function Spinner() {
  return (
    <Fragment>
      <img src={spinner} alt="Loading..." />
    </Fragment>
  )
}

export default Spinner
